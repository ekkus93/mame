# MTR-015 qualification trigger

This inert marker intentionally places the final remediation candidate inside the `tauri/**` workflow path filters so the complete Tauri project, security, and platform packaging matrix executes on one exact candidate head. It contains no production code or runtime configuration and may remain as audit evidence for the final qualification run.
